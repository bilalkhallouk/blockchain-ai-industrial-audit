{
  "name": "blockchain-audit-api",
  "version": "0.1.0",
  "private": true,
  "description": "Mock immutable audit log API for the industrial blockchain project.",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=18"
  }
}
